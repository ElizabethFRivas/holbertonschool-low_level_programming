#include "main.h"

/**
 * print_alphabet_x10 - Write a function that prints 10 times the alphabet
 *
 * Return: Always void.
 */

int _isalpha(int c)
{
	if ( (c >= 97 && c < 123) || (c >=65 && c <= 91))
		return 1; 
	return 0;
}	
	
